module.exports = {
  HID_CMD_UNKNOWN: 0,
  HID_CMD_GET_LAYERS: 1, // Host ask for layer data
  HID_CMD_GET_LAYERS_METADATA: 2, // Host asks for layer metadata (height, width, and count)
  HID_EVENT: 3, // Keyboard emits key event
};
