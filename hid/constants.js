export const HID_CMD_UNKNOWN = 0;
export const HID_CMD_GET_LAYERS = 1; // Host ask for layer data
export const HID_CMD_GET_LAYERS_METADATA = 2; // Host asks for layer metadata (height, width, and count)
export const HID_EVENT = 3; // Keyboard emits key event
